//import { readExcelFile } from '../utils/excelutils';
import { getTestData } from '../utils/testDataUtils';
import LoginPage from '../pages/LoginPage';
import { getLocators } from '../utils/locatorUtils';
import InsightsPage from '../pages/InsightsPage';


describe('TC_031 - Insights - Secondary Allocation Insights - Overview - Check Top 5 Owners by Secondary Unallocated graph and click on Bottom', () => {
    let loginPage;
    let insightsPage; 

    before(() => {
        // Load and set locators before any tests run
        getLocators('loginPage').then(locators => {
            loginPage = new LoginPage(locators);
        });

        getLocators('Insights').then(locators => {  
            insightsPage = new InsightsPage(locators); 
        });
    });

    it('TC_031 - Insights - Secondary Allocation Insights - Overview - check Top 5 Owners by Secondary Unallocated graph and click on Bottom', () => {
        getTestData('loginData', 'login').then(user => {
            loginPage.login(user.Companyname, user.email, user.password);
                insightsPage.clickonSecondaryInsights031();
                // loginPage.logout();         
            });
        });   

    });