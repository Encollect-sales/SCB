import { getTestData } from '../utils/TestDataUtils';
import LoginPage from '../pages/LoginPage';
import { getLocators } from '../utils/Locatorutils';
import ReportsPage from '../pages/ReportsPage';
 
describe('T31 -CustomerContactReport -  Check City Country, Region, Bom BucKet and Transcation From Date to Transaction To Date  and downlode report.', () => {
    let loginPage;
    let reportpage;
 
    before(() => {
        // Load and set locators before any tests run
        getLocators('loginPage').then(locators => {
            loginPage = new LoginPage(locators);
        });
 
        getLocators('ReportsPage').then(locators => {  
            reportpage = new ReportsPage(locators);
        });
    });
 
    it('T31 -CustomerContactReport - Check  Country, Region, Bom BucKet and Transcation From Date to Transaction To Date  and downlode report', () => {
        getTestData('loginData', 'login').then(user => {
            loginPage.login(user.Companyname, user.email, user.password);
                cy.wait(900);
                reportpage.Customercontactreport31();
               
                         
            });
        });
    });